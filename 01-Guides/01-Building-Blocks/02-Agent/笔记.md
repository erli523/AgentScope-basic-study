在AgentScope这个框架之中 很重要的一点就是 
"其是基于React来推动的" 也就是这里的
"一个无状态的推理-行动循环引擎，将模型、工具、权限系统、人机交互、上下文管理、中间件、状态管理和事件系统整合到一个统一接口中。"

主要负责
1、接受消息或者事件
2、管理上下文，包括上下文压缩 和 工具结果卸载 ？ 其实我觉得这里有一个问题  就是 为什么要agent自身来实现 这里的上下文压缩呢 ？ 就会让人觉得有点奇怪 ?


使用方式	谁负责判断压缩	谁生成压缩内容
Cursor 中的 OpenAI Codex 插件	Codex harness	OpenAI Responses API / Codex 模型
Cursor 原生 Agent + Codex 模型	Cursor Agent 框架	当前选择的模型
手动执行 /compress	用户主动触发	Cursor 调用模型完成

| 场景                     | 谁判断是否压缩             | 谁生成压缩内容 |
| ---------------------- | ------------------- | ------- |
| Codex Agent 内部         | Codex Agent Loop    | 模型或压缩服务 |
| Cursor 原生 Agent        | Cursor 平台的 Agent 框架 | 当前使用的模型 |
| 你自己开发 AgentScope Agent | 你实现的上下文管理模块         | 你调用的模型  |


"Agent 是智能体的核心抽象——一个无状态的推理-行动循环引擎，将模型、工具、权限系统、人机交互、上下文管理、中间件、状态管理和事件系统整合到一个统一接口中。"

这句话在我刚开始的学习之中 我其实把这里的重点落在了 无状态的推理-行动循环引擎 之上 。但是分析进一步思考之后 应该是落在了 “以循环作为核心抽象，并统一接入：”
        Message 和 Event；
        工具与权限控制；
        上下文压缩；
        中间件；
        人工确认与中断；
        状态和会话管理。
    但是这里这句话其实还是有一些问题 不太理解 


    对于一个智能体的配置而言

    我们需要从 
    name system_prompt model toolkit state model_config  offloader context_config react_config 
    其中 name system_prompt model 是必选项 其他的可以 不着急

在这里 reply返回一次性的结果 然后 reply_stream 返回的是一个流式的结果  
async def  main():
    msg = UserMsg(name="user", content="总结一下 README 的内容。")
    async for event in agent.reply_stream(msg):
        # 根据 `event.type` 分发处理 —— 每个分支对应一种事件类型。
        match event.type:
            case EventType.TEXT_BLOCK_DELTA:
                # 模型返回的流式文本片段 —— 追加到界面或标准输出。
                ...
            case EventType.TOOL_CALL_START:
                # 智能体即将调用工具 —— 展示调用信息。
                ...
            case _:
                # 其他事件：思考块、工具结果、回复结束等。
                ...
asyncio.run(main())

observe 只将消息注入智能体的上下文但是 不会触发replay的相关内容


人机交互 


AgentState 是一个 Pydantic 模型，保存了恢复智能体所需的全部信息——对话上下文、压缩摘要、权限规则、工具状态和当前 reply 位置。由于它是标准的 Pydantic 模型，可以序列化为 JSON 并存储在任意后端。

首先在这里  AgentState 是一个pydantic模型 保存了恢复一个智能体所需的全部信息。
因为是标准的pydatic模型 ，所以可以序列化为json 并存储在任意的后端

这里 AgentScope的结构可以简化为
class AgentState(BaseModel):
    session_id: str
    summary: str
    context: list[Msg]

    reply_context: ReplyContext
    permission_context: PermissionContext
    tool_context: ToolContext
    tasks_context: TaskContext

    middle_context: dict




首先这里我们初始化一个agent的时候 需要什么？
record= await storage.get_session(user_id, agent_id, session_id)
state=record.state if record else AgentState
这里的state可以是之前存在redius之中的也可以是我们 重新 
state=Agentstate的 


agent = Agent(
    name=
    system_prompt=
    model=DashScopeChatModel(
        crediential=
        model
    )
    state=state
)

        agent = Agent(
            name="my_agent",
            system_prompt="你是一个有帮助的助手。",
            model=DashScopeChatModel(
                credential=DashScopeCredential(api_key="YOUR_API_KEY"),
                model="qwen-max",
            ),
            state=state,
        )

result = await agent.reply(
    UserMsg(name= , content=)
)

await  storage.update_session_state(
            user_id=USER_ID,
            agent_id=AGENT_ID,
            session_id=SESSION_ID,
            state=agent.state,
        )


若 session 尚不存在，update_session_state 会抛出 KeyError。首次创建时请使用 upsert_session 建立 session 记录，后续轮次再切换为 update_session_state。
