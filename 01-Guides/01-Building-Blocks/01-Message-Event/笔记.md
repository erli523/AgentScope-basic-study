笔记的相关内容都是直接针对于 官网的内容进行学习整理的

首先是Message与 Event
这里


消息 — 智能体间通信与上下文持久化的基本单元。
事件 — 前后端交互与流式传输的基本单元，支持人工介入（Human-in-the-loop）场景。

消息只负责传递 消息 或者说内容  但是这里 
事件来负责告诉发生了什么

user_msg = UserMsg(
    name="user",
    content="帮我分析一下当前目录中的代码。",
)

官方提供的 UserMsg、AssistantMsg 和 SystemMsg 都支持字符串自动转换为 TextBlock。
也就是会包装为
user_msg =UserMsg(
    name="user",
    content=[
        TextBolock=(text="")
    ]
)

user_msg = UserMsg(
    name="user",
    content=[
        TextBlock(
            text="请分析这张系统架构图。",
        ),
        DataBlock(
            name="architecture.png",
            source=URLSource(
                url="https://example.com/architecture.png",
                media_type="image/png",
            ),
        ),
    ],
)
也就是说其他的内容需要我们来写在content之中

如何查看内容 
也就是
result =await agent.reply(msg)
for block in result.content :
    print(block.type)
    print(block) # 块的内容


获取工具调用
tool_calls = result.get_content_blocks(tool_call)
for call in tool_calls:
    print("工具名称：", tool_call.name)
    print("工具参数：", tool_call.input)
    print("工具状态：", tool_call.state)


AgentScope 没有名为 event_id 的字段，但每个事件都有自己的 id


async for event in agent.relpy_stream(msg):
    if isinstance(event,ReplayStartEvent):
        msg = AssistantMsg(name=event.name, content=[], id=event.reply_id)
    else:
        # 其他事件追加到消息，逐步还原状态
        msg.append_event(event)


    

    如何在前端接受事件并实时渲染 
    msg = None
async for event in agent.reply_stream(UserMsg("user", "帮我修复这个 bug")):
    if isinstance(event, ReplyStartEvent):
        msg = AssistantMsg(name=event.name, content=[], id=event.reply_id)

    elif isinstance(event, TextBlockDeltaEvent):
        print(event.delta, end="", flush=True)

    elif isinstance(event, ToolCallStartEvent):
        print(f"\n[正在调用 {event.tool_call_name}...]")

    elif isinstance(event, ToolResultEndEvent):
        print(f"[工具执行完成：{event.state}]")

    elif isinstance(event, ReplyEndEvent):
        print("\n[完成]")

    # 始终将事件追加到消息中
    if msg is not None:
        msg.append_event(event)


这里我能体会到的一点就是这里消息什么的都封装的比较方便 至少在获取相关的信息上 
是方便的